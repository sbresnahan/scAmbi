% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{set_feature_metadata}
\alias{set_feature_metadata}
\title{Harmonize Feature Metadata in Seurat Objects}
\usage{
set_feature_metadata(objs, file_path, assays = c("RNA", "RNA_corr"))
}
\arguments{
\item{objs}{A list of Seurat objects to be modified.}

\item{file_path}{Path to a tab-separated file where the first column contains
the new feature IDs (e.g., gene symbols). The order of IDs in this file must
match the order of features in the Seurat objects.}

\item{assays}{A character vector specifying which assays within each Seurat
object to update (e.g., \code{c("RNA", "RNA_corr")}).}
}
\value{
The input list of Seurat objects, with the \code{feature_meta} in the
specified assays modified in place.
}
\description{
Aligns feature metadata across multiple assays in a list of Seurat objects.
This function is useful for replacing existing feature IDs (e.g., Ensembl IDs)
with more human-readable ones (e.g., gene symbols) from an external file, while
preserving the original IDs in a separate column.
}
\details{
For each specified assay in each Seurat object, this function accesses the
feature metadata stored in \code{object[[assay]]@misc$feature_meta}. It performs
the following actions:
\enumerate{
\item Replaces the \code{rownames} of the \code{feature_meta} data frame with the IDs
from the supplied file.
\item Adds a new column named \code{feature_id} to the \code{feature_meta} data frame,
containing the original feature names from the assay's count matrix.
\item Clears the \code{rownames} of the modified \code{feature_meta} data frame.
}
This ensures that the original feature identifiers are not lost and can be
referenced later.
}
